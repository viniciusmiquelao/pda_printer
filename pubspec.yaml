name: pda_printer
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  charset_converter: ^2.1.1
  cupertino_icons: ^1.0.5
  esc_pos_printer: ^4.0.3
  esc_pos_utils: ^1.1.0
  flutter:
    sdk: flutter
  image: ^3.0.2
  intl: ^0.18.1
  path_provider: ^2.0.15
  ping_discover_network_forked: ^0.0.1
  qr_flutter: ^4.1.0
  flutter_wifi:
    git:
      url: https://github.com/huamoran/flutter_wifi.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0



flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/